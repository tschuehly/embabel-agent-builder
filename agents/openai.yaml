interface:
  display_name: "Embabel Agent Builder"
  short_description: "Build domain-first Embabel agents."
  default_prompt: "Use this skill to turn a prompt, product spec, feature spec, or existing domain model into a high-level Embabel agent spec first. Challenge ambiguous terms, produce a typed GOAP flow, decide states/tools/RAG/chatbots/subagents, fetch current Embabel docs on demand, then implement, test, and keep progress tracked in the spec."
